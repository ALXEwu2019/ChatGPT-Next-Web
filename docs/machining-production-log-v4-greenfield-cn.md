# 机加工车间生产日志管理系统 — 全新建设方案 v4

> **建设性质：绿场新建（Greenfield）**  
> 不修改、不依赖旧飞书多维表格中的 78+ 列、旧汇总自动化、旧工序编号（#40/#50）及历史公式。  
> 旧系统仅可作为业务参考或只读归档，不作为新系统数据源。

**核心原则：** 批号归生产，追溯号归质量；首道选管控批，下道选上道批；有效数汇总，对账看下发量。

---

## 1. 建设目标与边界

### 1.1 目标

| 目标 | 说明 |
| --- | --- |
| 生产报工 | 三产品、多工序、多区域按行报工，可汇总、可对账 |
| 质量闭环 | 不良 → 返工 → 品保确认 → 有效合格/有效报废 |
| 批次管控 | 已下发批号约束首道；下道只能接上道已产出批号 |
| 产量汇总 | 按工序规则汇总有效产量，**由 API 脚本定时同步，不用飞书公式聚合** |
| 问题追溯 | 完整追溯号独立生成，用于客诉/不良，**不参与生产汇总** |

### 1.2 绿场边界（明确不做）

| 不做 | 原因 |
| --- | --- |
| 修补旧主表横向产量列 | 架构根源问题 |
| 飞书自动化写汇总合计 | 与脚本双写、难维护 |
| 用追溯号选批/汇总 | 业务域混淆 |
| 工序表按产品重复行（#2030-STOPPER） | 配置冗余 |
| 下道手填跨产品/跨道批号 | 防串批 |

### 1.3 技术分工

| 能力 | 实现方式 |
| --- | --- |
| 追溯号、有效数公式 | 飞书公式 + 产品追溯规则表 |
| 选批约束、视图权限 | 飞书视图 + 关联筛选（静态） |
| 批工序汇总 | 飞书开放平台 API + `sync_batch_summary.py` + 服务器 cron |
| 不良/返工状态 | 主表状态字段 + 不良明细表 |
| 超产对账 | 对账视图 +（可选）脚本回写告警 |
| 表结构维护 | OpenClaw / BKD 助理 + 人工验收 |

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│ L0 配置层：产品表 / 工序表 / 产品工序路线表 / 产品追溯规则表      │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│ L1 计划层：入库批次管控（批号 × 产品 × 工序 × 本工序下发数量）    │
└────────────────────────────┬────────────────────────────────────┘
                             │ 仅首道工序选批
┌────────────────────────────▼────────────────────────────────────┐
│ L2 执行层：生产日志主表（一行一次报工）                          │
│           完整追溯号（公式只读，质量域）                          │
└───────┬────────────────────────────────────────────┬──────────┘
        │ 有不良                                          │
        ▼                                                 │
┌───────────────────┐                                     │
│ L3 质量层：不良明细 │                                     │
└─────────┬─────────┘                                     │
          ▼                                               │
┌───────────────────┐                                     │
│ 返工回填 + 品保确认│──→ 有效合格 / 有效报废 ──────────────┤
└───────────────────┘                                     │
                                                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ L4 汇总层：批工序产量汇总（脚本写入，飞书只读展示）                │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ L5 对账层：汇总合格 ≤ 本工序下发数量                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 核心概念

### 3.1 批号 vs 追溯号

| | 生产批号 | 完整追溯号 |
| --- | --- | --- |
| 用途 | 计划、选批、传批、汇总、对账 | 质量追溯、不良、客诉 |
| 示例 | `S-260617-A` | `S-260617-A-0624-MG02` |
| 参与汇总 | ✓ | ✗ |
| 下道选批 | ✓ 只选批号 | ✗ |

**关系：** 追溯号结构上含批号，业务上不能替代批号。

### 3.2 产量字段生命周期

| 字段 | 阶段 | 填写人 | 用途 |
| --- | --- | --- | --- |
| 合格数量、报废数量 | 初报 | 操作工 | 原始报工 |
| 返工后合格数、返工后报废数 | 返工后 | 班组长 | 返工结果 |
| 有效合格数量、有效报废数量 | 品保确认后 | 公式/确认 | **汇总与对账唯一来源** |

### 3.3 计入汇总条件（已定稿）

```
工序下发状态 = 已确认
且 有效合格数量、有效报废数量 已非空
```

**无不良时：** 有效合格数量 = 合格数量、有效报废数量 = 报废数量（公式自动生效，无需品保点确认）。

---

## 4. 三产品工序路线（定稿框架）

### 4.1 路线总表

| 产品 | 工序链 | 首道 | 末道 |
| --- | --- | --- | --- |
| STOPPER | #2030 → #4050 → #60 → #70 | #2030 | #70 |
| 止动块 | #4050 → #60 → #70 → #80 | #4050 | #80 |
| PTJ92 | 独立链（P2 后单开一轮） | 路线首道 | 待补 |

**共用工序：** #4050、#60、#70 两产品都经过，视图与汇总必须带 **产品** 隔离。

### 4.2 产品工序路线表（配置源）

| 产品 | 工序代码 | 顺序 | 是否首道 | 上道工序 | 汇总维度 | 批号来源 |
| --- | --- | --- | --- | --- | --- | --- |
| STOPPER | #2030 | 1 | 是 | — | 批号+合并区(A/B) | 入库批次管控 |
| STOPPER | #4050 | 2 | 否 | #2030 | 批号+区域(MG) | 上道批号池 |
| STOPPER | #60 | 3 | 否 | #4050 | 仅批号 | 上道批号池 |
| STOPPER | #70 | 4 | 否 | #60 | 仅批号 | 上道批号池 |
| 止动块 | #4050 | 1 | 是 | — | 批号+区域(MG) | 入库批次管控 |
| 止动块 | #60 | 2 | 否 | #4050 | 仅批号 | 上道批号池 |
| 止动块 | #70 | 3 | 否 | #60 | 仅批号 | 上道批号池 |
| 止动块 | #80 | 4 | 否 | #70 | 仅批号 | 上道批号池 |

### 4.3 批号传递（STOPPER 示例）

```
管控表选批 S-260617-A
  → #2030 报工（批号 S-260617-A）
  → #4050 只能选该批（批号不变，不选追溯号）
  → #60  只能选 #4050 已产出且已确认的批
  → #70  只能选 #60 已产出且已确认的批
```

止动块从 #4050 起，末道 #80，不经 #2030。

---

## 5. 追溯号规则（质量域）

**统一结构：** `生产批号 - 填报月日(MMDD) - 末段标识`

| 工序 | 样例 | 末段来源 | 说明 |
| --- | --- | --- | --- |
| #2030 | S-260617-A-0623-A1 | 生产区域 A1/A2 | 每区每报工日行 |
| #4050 | S-260617-A-0624-MG02 | 生产区域 MG02/MG03 | 末段为 MG 区 |
| #60 | S-260617-A-0625-J1 | 工位 J1/J2 | 仅追溯；汇总不按 J 拆 |
| #70 | — | — | **不需要追溯号**（汇总仅批号） |
| #80 | — | — | **不需要追溯号**；以批号为准，不设现场末段 |

**公式（主表）：**

```text
完整追溯号 = 批号文本 & "-" & 填报月日 & "-" & 末段字段
```

末段字段来源由 **产品追溯规则表** 按 `产品 + 工序代码` 配置，不用工序名称匹配。

---

## 6. 汇总规则（生产域）

### 6.1 原则

- 数据源：主表 `有效合格数量`、`有效报废数量`
- 同步：`sync_batch_summary.py`（cron），**关闭飞书汇总自动化**
- 汇总键：**不含追溯号、不含月日**（除配置明确要求外）

### 6.2 脚本聚合配置（初始）

```json
{
  "aggregation_by_process": {
    "#2030": ["batch_text", "product", "process_code", "summary_area"],
    "#4050": ["batch_text", "product", "process_code", "production_area"],
    "#60": ["batch_text", "product", "process_code"],
    "#70": ["batch_text", "product", "process_code"],
    "#80": ["batch_text", "product", "process_code"]
  },
  "region_merge": {
    "#2030": { "A1": "A", "A2": "A", "B1": "B", "B2": "B" }
  },
  "default_aggregation": ["batch_text", "product", "process_code"]
}
```

说明：
- `product` 用于共用工序时防 STOPPER/止动块误合并。
- `#2030` 报工仍填 A1/A2/B1/B2，脚本汇总前映射为合并区 A/B。

### 6.3 汇总表示例

| 批号文本 | 产品 | 工序 | 生产区域 | 合格合计 | 报废合计 | 批工序键 |
| --- | --- | --- | --- | --- | --- | --- |
| S-260617-A | STOPPER | #4050 | MG02 | 500 | 0 | S-260617-A-#4050-MG02 |
| S-260617-A | STOPPER | #60 | （空） | 350 | 2 | S-260617-A-#60 |

### 6.4 #2030 区域合并规则（已定稿 A1）

报工粒度仍为 A1/A2/B1/B2；**汇总时合并为大区**：

| 报工生产区域 | 汇总合并区 | 汇总键示例 |
| --- | --- | --- |
| A1、A2 | A | `S-260617-A-#2030-A` |
| B1、B2 | B | `S-260617-A-#2030-B` |

脚本在分组前执行 `region_merge` 映射；追溯号末段仍用原始 A1/A2（质量域），与汇总键分离。

---

## 7. 数据模型（全新表清单）

### 7.1 配置层

#### 产品表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 产品代码 | 文本 | 唯一 |
| 产品名称 | 文本 | STOPPER / 止动块 / PTJ92 |
| 启用状态 | 单选 | 启用/停用 |

#### 工序表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 工序代码 | 文本 | #2030、#4050… **无产品后缀** |
| 工序名称 | 文本 | 车床、磨床… |
| 启用状态 | 单选 | |

#### 产品工序路线表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 产品 | 关联产品表 | |
| 工序代码 | 关联工序表 | |
| 顺序 | 数字 | 1,2,3… |
| 是否首道 | 复选框 | |
| 上道工序 | 关联工序表 | 首道为空 |
| 汇总维度 | 单选 | 仅批号 / 批号+区域 / 批号+工位(仅追溯) |
| 批号来源 | 单选 | 管控表 / 上道批号池 |

#### 产品追溯规则表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 产品 | 关联 | |
| 工序代码 | 关联 | |
| 段1来源 | 公式说明 | 通常批号文本 |
| 段2来源 | 公式说明 | 通常填报月日 |
| 段3来源 | 字段名 | 生产区域 / 工位代码 等 |

### 7.2 计划层

#### 入库批次管控表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 批号文本 | 文本 | 如 S-260617-A |
| 产品 | 关联 | |
| 工序代码 | 关联 | **本行下发针对哪道工序** |
| 本工序下发数量 | 数字 | 对账上限 |
| 生产区域 | 文本 | 可选；#4050 按 MG 区分时使用 |
| 状态 | 单选 | 未下发/已下发/已结案 |
| 下发时间 | 日期时间 | |

**粒度：** 一行 = 批号 + 产品 + 工序 +（可选区域）+ 下发数量。

### 7.3 执行层

#### 生产日志主表

| 字段组 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 标识 | 日志编号 | 自动编号 | |
| 计划 | 生产批号 | 关联/文本 | 业务批号 S-260617-A |
| | 产品 | 关联 | |
| | 工序代码 | 关联 | |
| | 关联管控批 | 关联 | 首道选入库批次 |
| | 上道批号 | 关联/查找 | 下道选批（只选批号） |
| 区域 | 生产区域 | 单选/文本 | #2030 A1/A2；#4050 MG02 |
| | 工位代码 | 文本 | #60 J1，仅追溯 |
| 产量 | 合格数量 | 数字 | 操作工填 |
| | 报废数量 | 数字 | |
| | 返工后合格数 | 数字 | 班组长填 |
| | 返工后报废数 | 数字 | |
| | 有效合格数量 | 公式 | 无不良时=合格数量；有不良返工后由品保确认生效 |
| | 有效报废数量 | 公式 | 无不良时=报废数量；有不良返工后由品保确认生效 |
| 状态 | 工序下发状态 | 单选 | 见 §8 状态机 |
| 质量 | 完整追溯号 | 公式 | 只读 |
| 人员 | 操作工、班次 | 关联 | |
| 校验 | 是否异常 | 公式 | 超产/超上道等 |
| 镜像 | 批号文本 | 公式 | = 生产批号显示值 |
| | 填报月日 | 公式 | MMDD |

**不包含：** 旧系统横向区域产量列、汇总合格合计列、选择上道汇总关联（改为上道批号池）。

### 7.4 质量层

#### 不良明细表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 关联生产记录 | 关联主表 | 单条报工行 |
| 生产批号 | 查找引用 | |
| 完整追溯号 | 查找引用 | 可选，定位用 |
| 不良数量 | 数字 | |
| 不良原因 | 关联不良原因库 | |
| 状态 | 单选 | 待返工/已返工/已确认 |

返工完成清单：P2 可与不良明细合并或保持轻量关联。

### 7.5 汇总层

#### 批工序产量汇总表（脚本写入）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 批号文本 | 文本 | |
| 产品 | 文本/关联 | |
| 工序代码 | 文本 | |
| 生产区域 | 文本 | #4050 必填；#60 可空 |
| 合格合计 | 数字 | 脚本写入 |
| 报废合计 | 数字 | 脚本写入 |
| 批工序键 | 文本 | 唯一键 |
| 末次同步时间 | 日期时间 | 脚本写入 |
| 同步批次号 | 文本 | 可选，运维用 |

**不设：** 关联生产记录双向关联、飞书公式聚合字段。

### 7.6 对账层（视图/公式）

关联 `入库批次管控` 与 `批工序产量汇总`：

```text
对账差异 = 合格合计 - 本工序下发数量
是否超产 = 对账差异 > 0
```

#4050 无区域下发时：`SUM(同批各区合格) ≤ 批+工序总下发量`。

---

## 8. 状态机（生产日志主表）

```
待报工 → 已报工
       → 待返工（有不良明细）
       → 待品保确认（返工数已填）
       → 已确认 / 已审核（有效数生效，可计入脚本汇总）
```

| 状态 | 可计入汇总 | 可被选为上道批号池 |
| --- | --- | --- |
| 已报工 | 否（默认） | 待确认 D1 |
| 已确认 | 是 | 是（且有效合格>0） |
| 已审核 | 是（若启用） | 是（且有效合格>0） |

---

## 9. 视图与角色（全新建）

| 角色 | 视图 | 批号来源 | 必填 |
| --- | --- | --- | --- |
| STOPPER #2030 工 | STOPPER·#2030 报工 | 入库批次管控 | 区域 A1/A2 |
| STOPPER #4050 工 | STOPPER·#4050 报工 | 上道批号池(#2030) | MG 区 |
| STOPPER #60 工 | STOPPER·#60 报工 | 上道批号池(#4050) | 工位 J（追溯） |
| STOPPER #70 工 | STOPPER·#70 报工 | 上道批号池(#60) | 待确认 |
| 止动块 #4050 工 | 止动块·#4050 报工 | 入库批次管控 | MG 区 |
| 止动块 #60/#70/#80 | 对应视图 | 上道批号池 | 按工序 |
| 品保 | 不良明细填报 | — | 批号+可选追溯号 |
| 班组长 | 待返工/返工回填 | — | |
| 品保 | 待确认 | — | |
| 计划/管理 | 批工序汇总、对账超标 | 只读 | |

每个视图固定：**产品 + 工序 + 批号来源 + 必填字段**。

### 9.1 下道选批实现（不用动态「当前记录」筛选）

飞书关联字段若不支持「产品=当前记录」动态筛选，采用：

**方案 1（推荐）：分产品视图 + 静态筛选**

- 止动块·#4050 视图：`上道批号` 关联筛选 `产品=止动块` + `状态=已确认` + `有效合格>0`
- STOPPER·#4050 视图：同理

**方案 2：上道批号池辅助表**

脚本或公式定时生成「可选批号」列表，主表关联该表。

**方案 3：批号文本只读 + 自动化**

选上道生产记录后自动带出批号，禁止手改。

---

## 10. 汇总脚本架构（新建）

### 10.1 组件

```
sync_batch_summary.py
├── 读取 config.json（聚合规则、表 ID、过滤状态）
├── 拉取生产日志主表（有效数非空 + 状态=已确认）
├── 按 aggregation_by_process 分组求和
├── 写入批工序产量汇总（upsert by 批工序键）
└── 日志 + 可选告警（超产）
```

### 10.2 运行

| 项 | 建议 |
| --- | --- |
| 频率 | 30min（可调 E1） |
| 部署 | 服务器 cron |
| 飞书侧 | 关闭同名汇总自动化 |
| 验收 | `--dry-run` 输出与 SQL 对账 |

### 10.3 仓库落位（待建）

```
feishu-batch-summary-sync/
├── sync_batch_summary.py
├── config.json
├── requirements.txt
└── OPENCLAW_HANDOFF.md
```

---

## 11. 实施阶段（绿场）

| 阶段 | 内容 | 交付物 |
| --- | --- | --- |
| **P0** | 新建空库 + 配置层 4 表 + 工序/路线初始数据 | 表结构验收 |
| **P0** | 主表 + 追溯规则 + 完整追溯号公式 | 25 条追溯非空 |
| **P0** | 汇总脚本 + cron + 汇总表 | 脚本 dry-run 通过 |
| **P1** | 入库批次管控 + 首道/下道视图 | 选批不串批 |
| **P1** | 脚本按 #4050/#60 跑通 + 对账视图 | 超产可见 |
| **P2** | 不良闭环 + 状态机 + 有效数公式 | 返工后汇总准确 |
| **P2** | STOPPER/止动块 #70/#80 视图与规则 | 末道可报工 |
| **P3** | 报工产量明细作汇总源（可选） | 主表瘦身 |
| **P3** | PTJ92 全链配置 | 第三产品上线 |

**与旧系统关系：** P0 起使用**全新飞书多维表格应用**（与旧 Base 完全隔离、新库命名）；旧表只读参考，不做迁移写回。PTJ92 全链在 P2 完成后单开一轮。

---

## 12. 决策登记表（已定稿）

| 编号 | 问题 | 结论 |
| --- | --- | --- |
| A1 | #2030 汇总维度 | **A1/A2→A，B1/B2→B**（合并大区，不按 A1/A2 拆行） |
| A2 | #70、#80 汇总 | **同 #60，仅批号+工序** |
| A3 | #4050 对账 | 待 P1 落地（管控按 MG 分行 vs 批+工序总量） |
| A4 | 计入汇总时机 | **仅「已确认」** |
| B1 | #60 末段 | 工位 J1/J2（仅追溯，不参与汇总） |
| B2 | #70、#80 追溯 | **不需要** |
| B3 | PTJ92 全链 | **P2 后单开一轮** |
| B4 | 检测批次号与追溯号 | 追溯号独立，不参与汇总/选批 |
| B5 | #80 现场末段 | **不需要**，以批号为准 |
| C1 | 无不良有效数 | **自动有效数=合格数**（无需品保点确认） |
| C2 | 有效合格计算 | 待 P2 不良闭环细化 |
| C3 | 不良/返工表结构 | 待 P2 |
| C4 | 多条不良明细 | 待 P2 |
| D1 | 上道批号池条件 | **已确认 + 有效合格>0** |
| D2 | 下道选批 | **只认批号，不区分上道 MG/A 区** |
| D3 | 管控表粒度 | 批×产品×工序×下发量（P1） |
| E1 | cron 频率 | 30min（可调） |
| E2 | 超产告警 | 对账视图优先；脚本告警可选 |
| E3 | 新库策略 | **全新多维表格应用，与旧库完全隔离** |

---

## 13. 协作节奏（当前）

1. ~~P0 建库 + 汇总脚本~~ ✓
2. **P1 收尾**：`docs/p1-completion-checklist-cn.md` + `docs/feishu-p1-manual-setup-cn.md`
3. **P1 验收**：`python verify_p1.py` + 界面筛选手工项
4. **定时同步**：`docs/p1-cron-setup-cn.md`（每天 8 / 12 / 20 点）
5. **P2**：`docs/openclaw-p2-prompt-cn.md`（不良闭环）

---

## 14. 定稿检查清单（新系统上线前）

- [ ] 新飞书 Base 与旧系统物理隔离
- [ ] 批号、追溯号字段分离，视图文案不混用
- [ ] 工序表无产品后缀；路线在产品工序路线表
- [ ] 首道选管控，下道选上道批号（非追溯号）
- [ ] 汇总表仅脚本写入，无飞书聚合自动化
- [ ] 汇总键不含追溯号；#4050 含 MG；#60 不含 J
- [ ] 管控表：批×产品×工序×下发量
- [x] §12 决策项已逐项确认（见 §12 定稿表）
- [ ] STOPPER / 止动块 路线与文档一致（无 #50）

---

*文档版本：v4-greenfield-r1 · §12 已定稿 · 配套 OpenClaw P0 指令单与汇总脚本*
