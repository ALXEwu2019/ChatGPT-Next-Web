# 2026 Base v4 优化 · 飞书 AI 一键 Prompt

> **用法：** 先粘贴 `docs/openclaw-2026-context-supplement-cn.md` 全文，再粘贴下方 **BEGIN～END** 给飞书 AI。  
> **目标 Base：** 机加工车间生产日志管理系统（新）2026（`P2MtbRCz1a0Pj8sAOtocrHb6ntf`）

---

## Prompt（复制以下整段）

<!-- OPENCLAW_2026_OPTIMIZE_BEGIN -->

你是飞书多维表格实施助手。请在本 Base（app_token=`P2MtbRCz1a0Pj8sAOtocrHb6ntf`）上，按 **v4 绿场最终方案** 分步优化，**不要**新建第二个应用。

### 总原则
- 批号归生产，追溯号归质量；首道选管控批，下道选上道批；有效数汇总，对账看下发量。
- 汇总只由外部脚本 `sync_batch_summary.py` 写入 `批工序产量汇总`（`tblIygS9QW3mPmIu`），禁止主表公式聚合、禁止「选择上道汇总」关联。
- 工序表使用通用代码 `#2030`/`#4050`/`#60`/`#70`/`#80`，禁止 `#2030-STOPPER` 式重复行（路线在产品工序路线表）。
- 隐藏废弃视图：#40、#50（STOPPER/止动块/PTJ92 的旧检测出库视图）。

### 本次任务（按顺序执行，每步完成后列出变更清单）

**步骤 1 — 字段瘦身（主表 `tblolmz13JUyLDFO`）**
1. 隐藏或删除以下字段（若视图仍引用则先改视图）：
   - `#4050选择上道汇总`（fld2Ylxtlr）、`#60选择上道汇总`
   - `#4050汇总批号`、`#4050汇总合格合计`、`#4050本汇总剩余可用数`、`#4050数量校验` 及 #60 同类
   - `车床简化批号`（fldQ3UVcGI）、`汇总批工序键`
2. 保留：`批号文本`、`有效合格数量`、`有效报废数量`、`工序下发状态`、`完整追溯号`。

**步骤 2 — 新建 per-view 选批字段（参考绿场 v4）**
在主表新增（若不存在）：
- `关联管控批_STOPPER#2030`（关联→入库批次管控表 tbltHNZWMrKAJGWh）
- `关联管控批_止动块#4050`
- `上道批号_STOPPER#4050`、`上道批号_STOPPER#60`、`上道批号_STOPPER#70`
- `上道批号_止动块#60`、`上道批号_止动块#70`、`上道批号_止动块#80`

**步骤 3 — 视图筛选（8 个报工视图）**

首道 2 个 — 关联管控批字段筛选（管控表）：
| 视图 | view_id | 条件 |
| --- | --- | --- |
| STOPPER-#2030报工 | vewK5AzZee | 产品=recyB8Z4Hljubu；工序=#2030；批号状态=已下发 |
| 止动块-#4050磨床生产日志 | vewEah4EhT | 产品=recsxLFXuAXXI0；工序=#4050；批号状态=已下发 |

下道 6 个 — 上道批号字段筛选（主表自身关联）：
- 条件：**工序下发状态=已确认** + **工序代码=上道工序** +（止动块视图加产品=止动块）
- **不要**添加「有效合格数量>0」
- 取消上道批号「允许多条」，改为单选

| 视图 | view_id | 上道工序 |
| --- | --- | --- |
| STOPPER#4050生产日志 | vew5Rb9Urh | #2030 recAV0kYI0r5LS |
| STOPPER#60检测机生产日志 | vew121aeT9 | #4050 recpGjOr9LdIrW |
| STOPPER#70出库填报单 | vew2SbrO7V | #60 recoEwR8aH1qZ2 |
| 止动块-#60检测机生产日志 | vew3B1ivXN | #4050 recv94Wg644L3s |
| 止动块-#70外观检生产日志 | vewbP2DKMa | #60 rectBainbnqqi0 |
| 止动块-#80出库填报单 | vew5WATrGc | #70 recFtCShkvImYO |

**步骤 4 — 隐藏废弃视图**
隐藏 view_id：`vewutGRS7H`、`vew9iD0uOM`、`vewIhOXmBg`、`vewNTBCgpS`、`vew11lWJej`

**步骤 5 — 管控表补字段（tbltHNZWMrKAJGWh）**
- 新增 `批号文本`（从批次号提取，与主表一致）
- 新增 `合格合计`：查找引用汇总表 `tblIygS9QW3mPmIu` 的 `合格合计`，匹配批号文本+产品+工序代码
- 确认有 `本工序下发数量` 或映射 `计划数量`

**步骤 6 — 汇总表只读化**
- 汇总表 `合格合计`/`报废合计` 改为脚本写入；删除主表内一切汇总公式列
- 汇总表可增加 `末次同步时间`、`同步批次号` 文本字段供脚本写入

### 输出要求
每步输出 Markdown 表格：| 操作 | 对象 | 原值 | 新值 | 风险 |
最后给出「待人工/script 完成项」：cron 部署、verify_2026.py 验收。

### 禁止
- 重建新 Base；修补绿场 token CHNKbTKTCaWbQis1vVXcsLvsnDh
- 恢复 #40/#50 为 STOPPER/止动块主工序链
- 用追溯号选批或汇总

<!-- OPENCLAW_2026_OPTIMIZE_END -->
