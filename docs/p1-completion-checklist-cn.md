# P1 收尾清单

> P0 已完成 · 汇总脚本已跑通 · 按顺序打勾

---

## A. 你必须在飞书界面完成的（约 30 分钟）

参考：**`docs/feishu-p1-manual-setup-cn.md`**

| # | 任务 | 状态 |
| --- | --- | --- |
| A1 | 6 个视图上道批号筛选（**去掉**有效合格>0 若保存不了） | ☐ |
| A2 | 取消「允许添加多个记录」（上道批号单选） | ☐ |
| A3 | 止动块视图加 **产品=止动块** 条件 | ☐ |
| A4 | 首道 2 视图：关联管控批筛选（已下发） | ☐ |
| A5 | 合格合计查找 **4 条件**（含生产区域） | ☐ |
| A6 | 生产批号各视图只读 | ☐ |

### 上道筛选推荐（止动块·#80 示例）

1. 产品 = 止动块  
2. 工序代码 = **#70**（点选工序表记录）  
3. 工序下发状态 = 已确认  
4. ~~有效合格数量 > 0~~（公式字段，P1 先不加）

---

## B. 服务器 / 脚本（可命令行）

```bash
cd feishu-batch-summary-sync
python3 verify_p1.py          # 自动验收
python3 sync_batch_summary.py --dry-run -v
python3 sync_batch_summary.py # 写入汇总
chmod +x run_sync.sh
# crontab: 0 8,12,20 * * * → ./run_sync.sh
```

详见 **`docs/p1-cron-setup-cn.md`**

---

## C. 自动验收（verify_p1.py）

| 检查项 | 说明 |
| --- | --- |
| 管控表≥4行 | 含 S-TEST-A 测试批 |
| 止动块产品关联 | 样例 3/4 行 |
| 汇总表有 S-TEST-A | 脚本已写入 |
| 主表有已确认记录 | 报工数据 |

---

## D. P1 通过后 → P2

打开 **`docs/openclaw-p2-prompt-cn.md`**，粘贴给飞书 AI 建不良明细与状态机。

---

## E. 已知问题与处理

| 问题 | 处理 |
| --- | --- |
| 生产批号公式为空 | 首道必须选管控批；下道必须选上道批号 |
| 上道筛选确定灰掉 | 去掉有效合格>0；工序用点选 |
| 对账合格合计为 0 | 批号一致 + 跑脚本 + 4 条件查找 |
| 主表多套上道/管控字段 | 每视图用各自专用字段，筛选规则相同 |

---

*P1 收尾 v1*
