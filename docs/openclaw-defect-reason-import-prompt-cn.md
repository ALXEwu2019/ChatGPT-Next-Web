# 不良原因库批量导入（飞书 AI Prompt）

> 将 `---BEGIN---` 到 `---END---` 复制给飞书 AI。  
> 目标表：**不良原因库**（`tblvX8KSv73TluVk`，机加工生产日志 v4）

---

---BEGIN---

请在「机加工生产日志 v4」的 **不良原因库** 中批量录入以下不良原因（共 **46 条**，全局唯一名称；不含处置类型）。

## 表结构要求

若表尚无下列字段，请先补齐：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 原因代码 | 文本 | 与原因名称相同即可（旧系统双列合并为一列） |
| 原因名称 | 文本 | 全局唯一；填报时下拉显示 |
| 原因类别 | 单选 | 可选：尺寸 / 外观 / 工艺 / 其他 |
| 启用状态 | 单选 | 选项：`启用`、`停用`；本批全部填 **启用** |

**不要**在原因库放「处置类型」。同一原因在不同产品/工序下可能既是返工又是报废，处置写在 **不良原因联动规则表**（见 `openclaw-defect-linkage-import-prompt-cn.md`）。

**不要**新建应用、不要删改生产日志主表与汇总脚本相关表。

## 导入规则

1. **原因代码** = **原因名称**（两列填相同文本）。
2. **启用状态** 全部 `启用`。
3. 若库中已有同名原因，**跳过不重复插入**。
4. 录入方式：批量新增记录；本表为纯文本/单选，**一行一名词、不重复**。
5. 产品×工序×返工/报废 的 137 条配置见 **不良原因联动规则表**（`openclaw-defect-linkage-import-prompt-cn.md`），不要写进本表。

## 数据清单（46 行，全局去重）

| 原因代码 / 原因名称 | 启用状态 |
| --- | --- |
| 24.61+0.04 | 启用 |
| 25.8±0.05 | 启用 |
| 25.95±0.04 | 启用 |
| 57.74±0.05 | 启用 |
| 57.75±0.04 | 启用 |
| L±0.05 | 启用 |
| R35.1±0.1 | 启用 |
| φ25.95±0.05 | 启用 |
| φ70.2±0.15 外径 | 启用 |
| φ24.61+0.04 | 启用 |
| φ70.2±0.15 外径大 | 启用 |
| 二次加工 | 启用 |
| 倒角 | 启用 |
| 其他 | 启用 |
| 内孔 | 启用 |
| 压伤 | 启用 |
| 同轴度大 | 启用 |
| 垂直度 | 启用 |
| 外观 | 启用 |
| 外观不良 | 启用 |
| 平行度 | 启用 |
| 总高 | 启用 |
| 总高 5.3 | 启用 |
| 总高 8.6 | 启用 |
| 总高 25.8±0.05 | 启用 |
| 撞刀 | 启用 |
| 放反 | 启用 |
| 生锈 | 启用 |
| 磕碰 | 启用 |
| 端面 | 启用 |
| 端面压伤 | 启用 |
| 齿伤 | 启用 |
| 齿部夹伤 | 启用 |
| 3.2 台阶 | 启用 |
| 3.2±0.1 | 启用 |
| R1.8 | 启用 |
| R1.8 圆弧 | 启用 |
| 不明批号 | 启用 |
| 坯料 | 启用 |
| 外径 15.3 | 启用 |
| 70.2 外径 | 启用 |
| 崩刀 | 启用 |
| 未磨出 | 启用 |
| 胚料 | 启用 |
| 跳工序 | 启用 |
| 夹伤 | 启用 |

## 操作步骤

1. 打开 **不良原因库**，确认有「原因代码、原因名称、启用状态」；**不要**加处置类型列。
2. 清空库中 **测试样例**（如「尺寸超差、材质问题」）若与正式数据冲突；保留已有正式行。
3. 按上表 **批量新增 46 行**（每个原因名仅 1 行）。
4. 建视图 **品保·不良原因词典**（筛选：启用状态=启用）。
5. 产品×工序×处置 的 137 条规则导入 **不良原因联动规则表**（另见 `openclaw-defect-linkage-import-prompt-cn.md`）。

## 完成后回报

1. 不良原因库总行数（目标 46）
2. 随机抽 3 行截图：原因名称 + 启用状态
3. 确认无重复原因名、无处置类型列

**禁止：** 重建应用、修改汇总脚本字段、改生产日志主表结构。

---END---

## 备注

- 「坯料」与「胚料」为不同词条，均保留。
- 联动规则 137 行、止动块#2030 停用策略见 `openclaw-defect-linkage-import-prompt-cn.md`。
