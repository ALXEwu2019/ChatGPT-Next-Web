# 不良原因联动规则表 · 修表 Prompt（飞书 AI）

> 数据已通过 API 导入 **137 行**（`feishu-batch-summary-sync/import_defect_linkage.py`）。  
> 若界面仍「像没内容」，多半是 **错列/错字段类型** 导致。复制 `---BEGIN---`～`---END---` 给飞书 AI **整理表结构**（不要删 137 行数据）。

---

---BEGIN---

请在「机加工生产日志 v4」的 **不良原因联动规则表**（`tblUyVVrhKQOu1pO`）**整理字段与视图**，不要删除已有 **137 条** 规则记录，不要重建应用。

## 问题说明

此前导入把数据写进了错误列：
- 工序长名称写进了「默认处置类型」
- 工序代码写进了「启用状态」
- 原因名写进了「备注」

API 已用正确关联字段重新导入。请让界面与下列结构一致。

## 应保留并展示的字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 规则编号 | 自动编号 | 主字段（若无则新建并设为主字段） |
| 规则名称 | 公式 | 关联产品 & 工序代码 & 不良原因 & 默认处置类型 |
| **关联产品** | 关联→产品表 | 显示 STOPPER / 止动块 / PTJ92 |
| **工序代码** | 关联→工序表 | 显示 #2030、#4050 等 |
| **不良原因** | 关联→不良原因库 | 显示原因名称 |
| **默认处置类型** | 单选 | 仅保留选项：**返工**、**报废** |
| **启用状态** | 单选 | 仅保留选项：**启用**、**停用** |
| 备注 | 文本 | 可选，留空 |

## 应隐藏或删除的错字段（勿动 137 行数据）

以下若为 **单选** 且选项被污染，请 **隐藏列** 或删除字段（数据已在正确列）：
- 旧「产品」（单选，非关联）
- 旧「不良类型」
- 被误用作原因/工序的「备注」单选列

**正确数据在：关联产品、工序代码、不良原因、默认处置类型、启用状态。**

## 视图（按正确字段重建筛选）

1. **默认·全部规则** — 显示全部 137 行  
2. **启用·按产品工序** — 启用状态=启用；分组：关联产品 → 工序代码  
3. **返工原因** — 启用 + 默认处置类型=返工  
4. **报废原因** — 启用 + 默认处置类型=报废  
5. **历史·止动块#2030** — 关联产品=止动块 且 工序代码=#2030（27 行，应为停用）

## 验收

- 总行数 **137**（启用 110，停用 27）  
- 任意一行能看到：关联产品、工序代码、不良原因、返工/报废、启用/停用  
- 「启用·按产品工序」视图不再为空  

完成后回报：截图默认视图 5 行 + 各视图行数。

---END---
