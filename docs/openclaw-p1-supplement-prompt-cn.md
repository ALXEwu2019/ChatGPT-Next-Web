# OpenClaw P1 补缺 Prompt（含 record_id）

> **场景：** P1 主表已部分建好，飞书 AI 因缺少 record_id / 字段结构失败。  
> 将 `---BEGIN---` 到 `---END---` **整段**复制给飞书 AI，**不要**让它重建应用或删表。

---

---BEGIN---

请在现有「机加工生产日志 v4」应用中 **仅完成剩余配置**，不要新建应用、不要删除已有表和测试数据。

## A. 环境与表 ID（已定，勿改）

| 对象 | ID |
| --- | --- |
| app_token | `CHNKbTKTCaWbQis1vVXcsLvsnDh` |
| 生产日志主表 | `tblXr4h68tqh2HDy` |
| 批工序产量汇总表 | `tblu1h0huPW1Mixd` |
| 产品表 | `tblntK7K89dOpJVp` |
| 工序表 | `tblxbzA1m4mS5rXf` |
| 入库批次管控表 | `tbl6bCLJThyUaD8U` |

## B. 产品 record_id（用于关联筛选）

| 产品代码 | 产品名称 | record_id |
| --- | --- | --- |
| STOPPER | 止动器 | `rechKic8YG1cTc` |
| ZHIDONG | 止动块 | `recvnmMdIn6lCo` |

## C. 工序 record_id（用于关联筛选）

| 工序代码 | record_id |
| --- | --- |
| #2030 | `recC9QvIgUH8oK` |
| #4050 | `recs76rS587WRW` |
| #60 | `recfoJ5q7gJVtK` |
| #70 | `recIZhFG8RKKMI` |
| #80 | `recvnmMevC5Dbr` |

## D. 批工序产量汇总表字段（脚本写入，只读）

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| 批号文本 | 文本 | 主键之一 |
| 产品 | 关联→产品表 | 关联字段 |
| 工序代码 | 关联→工序表 | 关联字段 |
| 合格合计 | 数字 | 脚本写入 |
| 报废合计 | 数字 | 脚本写入 |
| 批工序键 | 文本 | 唯一键，如 `S-TEST-A-#2030-A` |
| 末次同步时间 | 日期时间 | 脚本写入 |
| 同步批次号 | 文本 | 脚本写入 |

**无「生产区域」列**；#4050 的对账按管控表「生产区域」列与汇总键中的 MG 区人工对照。

## E. 已完成（不要重复、不要删除）

- 入库批次管控表 + 4 行样例 + 计划·管控维护
- 关联管控批 → 入库批次管控表
- 生产批号 公式（选管控批带出批号）
- 生产批号_上道 公式（选上道记录带出批号）
- 止动块 #4050/#60/#70/#80 报工视图（已建则跳过）
- 管理·批工序对账视图 + 合格合计/对账差异/是否超产

## F. 待完成（本次只做这些）

### F1. 上道批号 · 各视图关联筛选

字段 **上道批号** 关联本表 `tblXr4h68tqh2HDy`（单选）。

在各报工视图，为 **上道批号** 配置「关联记录筛选」（**不要**按上道 `生产区域` 筛选）：

| 当前视图 | 上道批号需满足 |
| --- | --- |
| STOPPER·#4050 报工 | `工序代码`=`recC9QvIgUH8oK`(#2030)，`工序下发状态`=已确认，`有效合格数量`>0 |
| STOPPER·#60 报工 | `工序代码`=`recs76rS587WRW`(#4050)，已确认，有效合格>0 |
| STOPPER·#70 报工 | `工序代码`=`recfoJ5q7gJVtK`(#60)，已确认，有效合格>0 |
| 止动块·#60 报工 | `工序代码`=`recs76rS587WRW`(#4050)，已确认，有效合格>0 |
| 止动块·#70 报工 | `工序代码`=`recfoJ5q7gJVtK`(#60)，已确认，有效合格>0 |
| 止动块·#80 报工 | `工序代码`=`recIZhFG8RKKMI`(#70)，已确认，有效合格>0 |

若无法按关联字段筛选用 record_id，改用主表已有公式字段 **工序代码文本**、**产品名称** 做等价筛选。

### F2. 首道 · 关联管控批筛选

| 视图 | 关联管控批筛选 |
| --- | --- |
| STOPPER·#2030 | 产品=`rechKic8YG1cTc`，工序=`recC9QvIgUH8oK`，状态=已下发 |
| 止动块·#4050 | 产品=`recvnmMdIn6lCo`，工序=`recs76rS587WRW`，状态=已下发 |

### F3. 对账视图 · 合格合计查找

在 **入库批次管控表** 的「合格合计」查找字段，匹配规则：

- 批号文本 = 汇总表.批号文本
- 产品 = 汇总表.产品
- 工序代码 = 汇总表.工序代码

#4050 且管控行有 `生产区域`（MG02/MG03）时：对账按行比较；汇总侧对应 `S-xxx-#4050-MG02` 键的合格数（可由「合格合计_#4050」公式或手工说明）。

### F4. 生产批号字段只读

- 首道视图：`生产批号` 只读，来自关联管控批
- 下道视图：`生产批号` 只读，来自 `生产批号_上道` 或上道批号.批号文本

## G. 验收（完成后逐条回报）

1. 管控表 table_id：`tbl6bCLJThyUaD8U`
2. STOPPER·#4050 上道批号下拉是否出现 S-TEST-A（主表已确认且有效合格>0 的 #2030 记录）
3. 止动块管控样例第 3、4 行产品是否显示「止动块」
4. 管理·批工序对账：S-260617-A #2030 行「合格合计」是否 >0（汇总脚本已写入）
5. 截图或文字说明：任一上道批号视图的筛选条件列表

**禁止：** 重建应用、删除汇总表 3 行数据、给汇总表加自动化。

---END---

## 手工配置备选（飞书 AI 仍失败时）

在飞书界面：**生产日志主表** → 进入视图 → 点击 **上道批号** 字段设置 → **关联记录筛选**：

```
工序下发状态 等于 已确认
且 有效合格数量 大于 0
且 工序代码 等于 （选对应上道工序记录）
```

STOPPER·#4050 示例：工序代码选 `#2030` 那条记录。

---

## 当前 API 实测状态（供你对照）

| 项 | 状态 |
| --- | --- |
| 管控表 | `tbl6bCLJThyUaD8U`，4 行样例 |
| ZHIDONG 产品 | 已补 `recvnmMdIn6lCo` |
| 止动块管控行产品关联 | 已修复 |
| 汇总表 | 已有 3 行（S-TEST-A #2030-A / #4050-MG02 / #60） |
| 上道批号视图筛选 | **待你或 AI 按上文 F1 完成** |

---

*补缺 Prompt v1 · 数据截至 API 拉取*
